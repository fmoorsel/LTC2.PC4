import { emptyString } from './Constants'

export class DistrictMapping {
    public mapFeaturePointer: string = emptyString;
    public mapName: string = emptyString;
    public district: string = emptyString;
}
